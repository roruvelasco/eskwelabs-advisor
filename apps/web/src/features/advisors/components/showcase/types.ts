// ─── Types ────────────────────────────────────────────────────────────────────

export interface Advisor {
  id: string;
  name: string;
  description: string;
}

// ─── Theme tokens ─────────────────────────────────────────────────────────────

export interface ThemeTokens {
  bg: string;
  headingColor: string;
  bodyColor: string;
  mutedColor: string;
  cardBg: string;
  cardBorder: string;
  chipBorder: string;
  chipText: string;
  labelColor: string;
  dividerColor: string;
  accentFg: string;
  accentText: string;
  ctaBg: string;
  ctaHoverBg: string;
  navBg: string;
  navBorder: string;
  navText: string;
  navActiveText: string;
  navActiveBg: string;
  progressBar: string;
}
