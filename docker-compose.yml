services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: eskwelabs_advisor
    ports:
      - '5432:5432'
    volumes:
      - eskwelabs_advisor_postgres:/var/lib/postgresql/data
volumes:
  eskwelabs_advisor_postgres:
